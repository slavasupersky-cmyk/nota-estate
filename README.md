# NOTA Estate

Сайт nota.estate и рабочие страницы для клиентов и агентов. Статика — открывается в браузере, без сборки и зависимостей.

## Сайт

| Файл | Что это |
|---|---|
| `index.html` | Главная: три вопроса к дому, Красная линия, база, анкета, разборы, контакт |
| `karta.html` | Карта и база 288 домов с фильтрами по классу и итогу |
| `razbory.html` | Разборы: районы, дома, отказы, канал |
| `razbor-hamovniki.html` | Разбор района — Хамовники |
| `nota-index.html` | Индекс, ежемесячный выпуск (тёмная тема) |
| `podbor.html` | Подбор: анкета, как работаем, что вы получите |
| `doma/` | Карточки домов — собираются скриптом, руками не править |

`css/nota.css`, `js/nota.js` — общие стили и скрипты (фильтры, анкета, выпадающие списки).
`img/` — кадры сайта, лёгкие JPG. Исходники в `img/_src/` — в репозиторий не попадают.
`favicon.svg` — знак, лежит в корне: папка `icons/` попадает под `Icon?` в .gitignore.

## Сборка

`python3 tools/build.py` — пересобрать после любой правки общих частей:
- `tools/partials/header.html`, `footer.html` — шапка и подвал, раскладываются во все страницы сайта;
- `tools/site.json` — телефон, почта, каналы, офис, реквизиты ИП (подвал и политика);
- `tools/doma.json` — какие дома получают карточку и текст «На заметку:»; цифры берутся из `data/zhk-moskva-biznes-plus-geo.csv`.

Результат — обычные статичные HTML-файлы: поисковики и нейросети видят весь текст.

## Рабочие страницы

| Файл | Что это | Для кого |
|---|---|---|
| `nota-brif-podbor.html` | Бриф на подбор: 8 блоков вопросов, живой фильтр по базе 288 ЖК, подсказки «что уступить» | клиент |
| `nota-devyat-domov-shkola.html` | Девять домов со школой рядом — три отметки | клиент |
| `nota-klientu-tri-varianta.html` | Три варианта из восьми | клиент |
| `nota-razbor-studentka.html` | Разбор подборки для студентки — 12 разделов, семь строк | агент |
| `nota-razbor-podborka.html` | Семейный разбор — планировки, карта, школы | агент |
| `nota-shkoly-postuplenie.html` | Школы: правила приёма, конкурсы, цены частных | агент |
| `nota-karta-zhk.html` | Интерактивная карта новостроек с фильтрами по классу | агент |
| `shkoly-marshruty.html` | До школы пешком: карта пеших маршрутов от 288 домов до школ и обратно — минуты по улицам, корпуса, отметка NOTA, кнопки маршрутов Яндекса. Пересчёт — `tools/marshruty/` | агент |
| `nota-progulka-hamovniki.html` | Прогулка по Хамовникам: рамка лекции и 7 кластеров, 26 домов по маршруту на карте NOTA-стиля, фото и «что искать глазами» из презентации, заметки, ЦИАН, отчёт в личку. Закрыта от поиска | себе |

`assets/` — знак NOTA, карты, планировки.

## Данные

| Файл | Что это |
|---|---|
| `data/zhk-moskva-biznes-plus-geo.csv` | База 288 домов с координатами — источник для карты и карточек домов |
| `data/index-2026-09-shkoly-geo.csv` | Школы выпуска индекса 09 с координатами |
| `data/shkoly-baza-2026-09-23.json` | Снимок базы школ: 488 организаций, отметка NOTA |
| `data/zhk-shkoly-peshkom.csv` | Пары «дом — школа» до 30 минут пешком: минуты по улицам, корпус, ссылки на маршрут |
| `data/zhk-shkoly-svodka.csv` | По каждому дому: ближайшая школа, сколько школ за 10/15/20/30 минут, лучшая рядом |
| `data/shkoly-zhk-svodka.csv` | По каждой школе: новые дома за 10/15/20/30 минут |
| `data/shkoly-korpusa-geo.csv` | Корпуса школ с координатами |
| `data/marshruty-shkoly.md` | Как считали маршруты, цифры и ограничения |

## Публикация

Settings → Pages → Deploy from a branch → `main` / `(root)`.
Адрес: `https://<аккаунт>.github.io/nota-estate/`

## Чего здесь нет

Рендеры, презентации и исходники материалов в репозиторий не кладём — для них облако.
Ограничения GitHub: файл до 100 МБ, репозиторий желательно до 1 ГБ, а история хранит
все прошлые версии файлов навсегда. `.gitignore` не пустит тяжёлые форматы, даже если
они случайно окажутся в папке.
